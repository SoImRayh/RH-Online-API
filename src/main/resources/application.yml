spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: rayh
    password: 40028922
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true